<%- include("header") -%>

  <div id="box">
    <form action="/listName" method="post">
      <label for="cityInput">Enter List :</label>
      <input id="cityInput" type="text" name="ListName">
      <button type="submit" name="submit">+</button>
    </form>
  </div>

  <div class="box" id="heading">
    <h1> <%= listTitle %> </h1>
  </div>

  <div class="box">
    <% for (let i=0; i<newListItems.length; i++) { %>
      <form action="/delete" method="post">
        <div class="item">
          <input type="checkbox" name="checkbox" value="<%=newListItems[i]._id%>" onChange="this.form.submit()"><!--//acts like submit button to start post req-->
          <p><%=  newListItems[i].name  %></p>
        </div>
        <input type="hidden" name="listName" value="<%= listTitle %>">
      </form>
      <% } %> 
      <form class="item" action="/" method="post">
        <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
        <button type="submit" name="list" value = "<%= listTitle %>">+</button>
      </form>
  </div>

<%- include("footer") -%>
